/*
|--------------------------------------------------------------------------
| Authenticators
|--------------------------------------------------------------------------
|
| Define a list of authenticators used by the auth provider
|
*/

declare module '@adonisjs/auth/types' {
  interface Authenticators {
    // session: SessionGuard<SessionUserProvider<User>>
  }
}